SUPABASE_PROJECT_REF=ref
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321/
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=publishable_key

# Server-only — never prefix with NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=

# Cross-portal URLs (vendor preview iframe + buyer links)
NEXT_PUBLIC_BUYER_URL=http://localhost:3000
NEXT_PUBLIC_VENDOR_ORIGIN=http://localhost:3001

# Phase 8 — Groq AI Mode + image search (optional; falls back to keyword search)
GROQ_API_KEY=

# --- Phase 15: Infrastructure GO-LIVE (optional locally; required in prod) ---

# Sentry — same DSN ok for all 3 apps (tagged by portal)
SENTRY_DSN=

# PostHog — project API key (public by design)
NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Inngest — buyer app (apps/web) only
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# Resend — transactional email
RESEND_API_KEY=
RESEND_FROM_EMAIL=SourceByJay <noreply@sourcebyjay.com>
